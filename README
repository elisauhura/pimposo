Projeto de MAC0216

Vitor Pereira da Silva
NºUSP 10263531

Lucas Medeiros Sobrinho de Sousa
NºUSP 9833002

Estrutura do projeto:

    O projeto está organizado em módulos, cada módulo é composto de 3 arquivos:
    $MODULO.h -> caeçalho
    $MODULO.c -> implementação
    $MODULO.test.c -> teste

    O Makefile gerencia todo o projeto.

    Para compilar o modulo se usa: make $MODULO
    Para testar o modulo se usa: make test_$MODULO

    Os módulos implementados até o momento são o list, o hash e o element.

    Os módulos de list e hash foram feitos de forma genérica, assim podem ser usados com qualquer tipo de dados.

    O type.h não é um módulo, mas sim um arquivo para tipos de dados comum no projeto.

    `make listmods` deve listar os módulos disponíveis junto com seus test files.

Para testar o ep:

    Rode um `make test_all`

Estruturas de dados:

Cabeçalho da lista vai ser por ponteiro
Contém o numero de elementos

Hash vai ser:

for(int i = 0, hash = 0; str[i] != '\0'; i++)
    hash = hash << 4 + str[i]
return hash % n;

Hash table vai ser feita usando várias listas

TODO:
    Arrumar as estruturas (Vitor)
    Escrever a historia (Ambos)
    Apresentar descrição
    Listar elementos da sala
    Implementar animação
    5 salas no minimo e vários objetos
    Adicone verbos

    List
    PList.free
    PList.new
    PList....

    PTabSim.new(...)

    PList.of(PElem.base)

    PBase:
        new
        free
        class
        